import { ParamDirective } from './param.directive';

describe('ParamDirective', () => {
  it('should create an instance', () => {
    const directive = new ParamDirective();
    expect(directive).toBeTruthy();
  });
});
