import { AsdfDirective } from './asdf.directive';

describe('AsdfDirective', () => {
  it('should create an instance', () => {
    const directive = new AsdfDirective();
    expect(directive).toBeTruthy();
  });
});
